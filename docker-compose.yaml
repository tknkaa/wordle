services:
  db:
    image: postgres:17
    container_name: wordlepostgres
    ports:
      - 127.0.0.1:5432:5432
    volumes:
      - db-store:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "passw0rd"
      POSTGRES_DB: "mydb"

volumes:
  db-store:
